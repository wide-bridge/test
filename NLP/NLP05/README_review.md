# AIFFEL Campus Online Code Peer Review Templete
- 코더 : 양선호
- 리뷰어 : 신기주


# PRT(Peer Review Template)
- [x]  **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요?**
    - 문제에서 요구하는 최종 결과물이 첨부되었는지 확인
        - SFT → RM → PPO 3단계 파이프라인이 모두 구현되어 있으며, 각 단계의 결과물(모델 저장, 정량 평가, 정성 분석)이 노트북 내에 기록되어 있다.
        - SFT: `skt/ko-gpt-trinity-1.2B-v0.5` 기반, LoRA 적용, ROUGE-L `0.0964` 기록
        - RM: KorQuAD 1.0 증강 데이터 포함 학습 완료
        - PPO: ROUGE-L `0.0903`, BERTScore F1 `0.6747` 기록
        - 특히 기본 `kogpt2-base-v2` 대신 **1.2B 파라미터의 Trinity 모델로 Foundation Model을 교체**한 점은 과제 요구를 넘어선 시도다.
        
        > 📸 근거: 노트북 셀 [14] SFT 정량 평가 리포트 (ROUGE-L: 0.0964), 셀 [29] PPO 정량 평가 (ROUGE-L: 0.0903, BERTScore F1: 0.6747)
            
- [x]  **2. 전체 코드에서 가장 핵심적이거나 가장 복잡하고 이해하기 어려운 부분에 작성된 
주석 또는 doc string을 보고 해당 코드가 잘 이해되었나요?**
    - 해당 코드 블럭을 왜 핵심적이라고 생각하는지 확인
    - 해당 코드 블럭에 doc string/annotation이 달려 있는지 확인
    - 해당 코드의 기능, 존재 이유, 작동 원리 등을 기술했는지 확인
    - 주석을 보고 코드 이해가 잘 되었는지 확인
        - **[Phase 1] LoRA 설명 마크다운 (셀 5)** 이 가장 잘 작성된 부분이다. 파라미터 수까지 명시하며 3단계 구조를 명확하게 설명했다:
        ```
        - 1단계: 원본 모델 가중치 (12억 개 파라미터) → Freeze
        - 2단계: LoRA용 얇은 레이어 (약 1,100만 개 파라미터) → Unfreeze (학습 가능)
        - 3단계: 합치기 (Inference)
        ```
        - **셀 [8] 데이터 검증 코드**도 `input_ids`와 `labels`를 실제로 디코딩해서 확인하는 구조로, 학습 대상이 올바른지 검증하는 의도가 명확하게 드러난다.
        - 다만 PPO 학습 관련 셀(24~27)은 코드 주석이 상대적으로 빈약하다. Actor/Critic 분리 구조의 의미가 마크다운으로 설명되어 있지 않아 아쉽다.
        
        > 📸 근거: 셀 [5] Phase 1 Foundation Model 교체 및 LoRA 설정 마크다운, 셀 [8] 학습 데이터 검증 코드

        
- [x]  **3. 에러가 난 부분을 디버깅하여 문제를 해결한 기록을 남겼거나
새로운 시도 또는 추가 실험을 수행해봤나요?**
    - 문제 원인 및 해결 과정을 잘 기록하였는지 확인
    - 프로젝트 평가 기준에 더해 추가적으로 수행한 나만의 시도, 실험이 기록되어 있는지 확인
        - **① Foundation Model 교체 (가장 차별화된 시도)**  
          기본 제공 `kogpt2-base-v2` (125M)를 `ko-gpt-trinity-1.2B-v0.5` (1.2B)로 교체했다. 단순 교체가 아니라 LoRA로 메모리 문제를 해결하는 구조까지 함께 설계했다. 발표에서 직접 언급했듯 "모델 사이즈가 커서 loss 2점대도 양호한 수준"이라는 판단도 적절하다.
        - **② SFT/RM/PPO 입력 포맷 통일**  
          세 단계 모두 동일한 `PROMPT_TEMPLATE`을 사용하도록 설계했다. 입력 포맷이 불일치하면 PPO 학습 시 RM 점수가 왜곡되는 문제가 생기는데, 이를 미리 방지한 설계다.
        - **③ 데이터 정제 → 증강 사이클**  
          EDA에서 전체 데이터의 1/3을 제거하고 KorQuAD 1.0 데이터 1,000개를 추가 보강하는 과정이 Phase 0과 RM EDA 마크다운에 상세히 기록되어 있다.
        
        > 📸 근거: 셀 [2] CFG의 `MODEL_NAME: "skt/ko-gpt-trinity-1.2B-v0.5"`, 셀 [4] Phase 0 SFT EDA 마크다운, 셀 [18] RM EDA 마크다운, 셀 [25] PPO 데이터 로드 시 동일 포맷 적용 주석
        
- [x]  **4. 회고를 잘 작성했나요?**
    - 주어진 문제를 해결하는 완성된 코드 내지 프로젝트 결과물에 대해
    배운점과 아쉬운점, 느낀점 등이 기록되어 있는지 확인
    - 전체 코드 실행 플로우를 그래프로 그려서 이해를 돕고 있는지 확인
        - 셀 [31] 회고에서 배운 점, 잘한 점, 아쉬운 점이 구체적으로 구분되어 있다. 특히 인상적인 부분:
        > "데이터가 많지 않은 상태에서 소수의 쓰레기가 있으면 훈련 시 영향을 많이 받는 것을 파악했습니다."
        - 이는 단순한 감상이 아니라 실제로 영문 노이즈 데이터가 그대로 출력되는 현상을 직접 경험하고 기록한 내용이다.
        - 아쉬운 점: **전체 실행 플로우 그래프가 없다.** SFT → RM → PPO 3단계 파이프라인의 전체 구조를 한눈에 보여주는 다이어그램이 있었다면 더 완성도 높은 회고가 됐을 것이다.
        
        > 📸 근거: 셀 [31] 회고 (Retrospective) 마크다운
        
- [x]  **5. 코드가 간결하고 효율적인가요?**
    - 파이썬 스타일 가이드 (PEP8) 를 준수하였는지 확인
    - 코드 중복을 최소화하고 범용적으로 사용할 수 있도록 함수화/모듈화했는지 확인
        - **잘한 부분:**  
          `CFG` 딕셔너리로 모든 하이퍼파라미터와 경로를 중앙 관리하는 구조가 깔끔하다. `IS_RM_TRAINING` 플래그로 재실행 시 불필요한 학습을 스킵하는 설계도 실용적이다.  
          메모리 관리도 명시적으로 처리했다:
          ```python
          if 'model' in globals(): del model
          gc.collect()
          torch.mps.empty_cache()
          ```
        - **개선점:**  
          셀 [10]에 `save_mered_sft` → `save_merged_sft` 오타 수정 코드가 직접 노출되어 있다. 주석으로 수정 이유를 남기는 것은 좋지만, 원본 오타 함수명이 주석에 그대로 남아있어 혼란을 줄 수 있다.
        
        > 📸 근거: 셀 [2] CFG 딕셔너리 전체 구조, 셀 [12] 메모리 정리 코드, 셀 [20] IS_RM_TRAINING 플래그


# 회고(참고 링크 및 코드 개선)
```
# 같은 RLHF 과제를 다른 방향으로 푼 코드를 보며 두 가지를 배웠다.
#
# 1. Foundation Model 교체 + LoRA 조합
#    우리(NLP05)는 kogpt2-base-v2를 그대로 썼지만,
#    선호님은 1.2B Trinity 모델로 교체하고 LoRA로 효율화했다.
#    모델 크기가 크면 loss 절대값이 높아도 수렴 중일 수 있다는 것,
#    LoRA가 그 해결책이 된다는 것을 이 코드로 재확인했다.
#
# 2. BERTScore 추가
#    우리는 BLEU/ROUGE만 썼는데, 선호님은 BERTScore F1까지 추가했다.
#    ROUGE의 한계(글자 일치 기반)를 정확히 짚고 의미론적 유사도 지표를
#    병행한 점이 평가 설계에서 한 단계 앞선 선택이었다.
#
# 참고: LoRA (Low-Rank Adaptation) - https://arxiv.org/abs/2106.09685
# 참고: BERTScore - https://arxiv.org/abs/1904.09675
```
